name: example
description: 'Flutter Bird'
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flare_engine:
    path: ../
  flutter:
    sdk: flutter
  haptic_feedback: ^0.6.4+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/audio/
